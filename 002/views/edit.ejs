<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Task</title>
  <style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #e0e7ff 0%, #c3dafe 100%);
  min-height: 100vh;
  padding: 20px;
  color: #333;
}


.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

h1 {
  text-align: center;
  color: #1e3a8a;
  margin-bottom: 20px;
  font-size: 2.5rem;
  font-weight: 600;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
}


form {
  background: #ffffff;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  max-width: 500px;
  margin: 0 auto 30px;
}

.form-table {
  width: 100%;
  border-collapse: collapse;
}

.form-table td {
  padding: 10px;
}

input[type="text"], input[type="date"], select, input[type="hidden"] {
  width: 100%;
  padding: 10px;
  border: 1px solid #d1d5db;
  border-radius: 5px;
  font-size: 1rem;
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

input[type="text"]:focus, input[type="date"]:focus, select:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 5px rgba(59, 130, 246, 0.5);
}

button[type="submit"] {
  width: 100%;
  padding: 12px;
  background: #3b82f6;
  color: white;
  border: none;
  border-radius: 5px;
  font-size: 1rem;
  cursor: pointer;
  transition: background 0.3s ease, transform 0.2s ease;
}

button[type="submit"]:hover {
  background: #2563eb;
  transform: translateY(-2px);
}

/* Back link styling */
.back-link {
  display: block;
  text-align: center;
  color: #1e3a8a;
  font-size: 1rem;
  text-decoration: none;
  margin-top: 20px;
  transition: color 0.3s ease;
}

.back-link:hover {
  color: #3b82f6;
  text-decoration: underline;
}

/* Responsive design */
@media (max-width: 768px) {
  h1 {
    font-size: 2rem;
  }

  .form-table td {
    padding: 8px;
  }

  input[type="text"], input[type="date"], select, button[type="submit"] {
    font-size: 0.9rem;
  }
}

@media (max-width: 480px) {
  .container {
    padding: 10px;
  }

  form {
    padding: 15px;
  }

  input[type="text"], input[type="date"], select, button[type="submit"] {
    font-size: 0.85rem;
  }

  .back-link {
    font-size: 0.9rem;
  }
}
  </style>
</head>
<body>
  <h1>Edit Task</h1>

  <form action="/updateData" method="post">
    <input type="hidden" name="id" value="<%= id %>" />

    <table class="form-table">
      <tr>
        <td>
          <input
            type="text"
            name="task_title"
            placeholder="Task Title"
            value="<%= getData.task_title %>"
            required
          />
        </td>
      </tr>
      <tr>
        <td>
          <input
            type="text"
            name="task_desc"
            placeholder="Task Description"
            value="<%= getData.task_desc %>"
            required
          />
        </td>
      </tr>
      <tr>
        <td>
          <select name="status" required>
            <option value="">Select Status</option>
            <option value="pending" <%= getData.status === "pending" ? "selected" : "" %>>Pending</option>
            <option value="in-progress" <%= getData.status === "in-progress" ? "selected" : "" %>>In Progress</option>
            <option value="completed" <%= getData.status === "completed" ? "selected" : "" %>>Completed</option>
          </select>
        </td>
      </tr>
      <tr>
        <td>
          <input
            type="date"
            name="deadline"
            value="<%= getData.deadline %>"
            required
          />
        </td>
      </tr>
      <tr>
        <td>
          <button type="submit">Update Task</button>
        </td>
      </tr>
    </table>
  </form>

  <a class="back-link" href="/">← Back to Task List</a>
</body>
</html>