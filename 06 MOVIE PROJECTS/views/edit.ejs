<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Movie</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Arial', sans-serif;
  background: #f4f4f4;
  color: #333;
  line-height: 1.6;
  padding: 1rem;
}

/* ======================================
   CSS Variables
====================================== */
:root {
  --primary: #3498db;
  --secondary: #2ecc71;
  --danger: #e74c3c;
  --light: #ecf0f1;
  --lighter: #bdc3c7;
  --dark: #34495e;
  --transition-speed: 0.3s;
}

/* ======================================
   Container & Layout
====================================== */
.container {
  width: 90%;
  max-width: 1200px;
  margin: 2rem auto;
  padding: 1rem;
}

/* ======================================
   Header Styles
====================================== */
header {
  text-align: center;
  margin-bottom: 2rem;
}

header h1 {
  font-size: 2.5rem;
  color: var(--dark);
}

header p {
  font-size: 1.1rem;
  color: var(--secondary);
}

/* ======================================
   Animation Classes
====================================== */
.animated {
  opacity: 0;
  transform: translateY(20px);
  animation: fadeInUp var(--transition-speed) forwards;
}

.delay-1 {
  animation-delay: 0.3s;
}

.delay-2 {
  animation-delay: 0.6s;
}

.delay-3 {
  animation-delay: 0.9s;
}

@keyframes fadeInUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ======================================
   Form Container & Grid
====================================== */
.form-container {
  background: #fff;
  padding: 1.5rem;
  border-radius: 6px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  margin-bottom: 2rem;
}

.form-container h2 {
  font-size: 1.8rem;
  margin-bottom: 1.5rem;
  text-align: center;
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1rem;
}

/* ======================================
   Form Elements
====================================== */
.form-group {
  display: flex;
  flex-direction: column;
}

.form-group label {
  margin-bottom: 0.5rem;
  font-weight: bold;
  color: var(--dark);
}

.form-group input,
.form-group textarea {
  padding: 0.75rem;
  border: 1px solid var(--lighter);
  border-radius: 4px;
  font-size: 1rem;
  transition: border-color 0.2s;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--primary);
}

/* Ensure full-width inputs (e.g., textarea) span across columns */
.form-group.full-width {
  grid-column: 1 / -1;
}

/* ======================================
   Buttons
====================================== */
button.btn,
a.btn {
  display: inline-block;
  padding: 0.6rem 1.2rem;
  border: none;
  border-radius: 4px;
  text-decoration: none;
  font-size: 1rem;
  cursor: pointer;
  transition: background var(--transition-speed);
}

.btn-primary {
  background: var(--primary);
  color: #fff;
  border: none;
}

.btn-primary:hover {
  background: #2980b9;
}

.btn-secondary {
  background: var(--secondary);
  color: #fff;
}

.btn-secondary:hover {
  background: #27ae60;
}

.btn-danger {
  background: var(--danger);
  color: #fff;
}

.btn-danger:hover {
  background: #c0392b;
}

/* ======================================
   Movie Table Styles (if applicable)
====================================== */
.movie-table-container {
  overflow-x: auto;
  background: #fff;
  border-radius: 6px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  padding: 1rem;
}

.movie-table {
  width: 100%;
  border-collapse: collapse;
}

.movie-table thead {
  background: var(--dark);
  color: #fff;
}

.movie-table thead th {
  padding: 0.75rem;
  text-align: left;
  font-weight: bold;
}

.movie-table tbody tr {
  border-bottom: 1px solid var(--lighter);
}

.movie-table tbody tr:nth-child(even) {
  background: var(--light);
}

.movie-table tbody td {
  padding: 0.75rem;
  vertical-align: top;
}

/* ======================================
   Movie Details & Actions
====================================== */
.movie-poster {
  width: 80px;
  height: auto;
  border-radius: 4px;
  margin-top: 0.5rem;
}

.movie-title {
  font-size: 1.1rem;
  font-weight: bold;
  margin-bottom: 0.5rem;
}

.movie-description {
  font-size: 0.9rem;
  color: var(--lighter);
}

.movie-director {
  font-size: 1rem;
}

.rating-badge {
  background: var(--primary);
  color: #fff;
  padding: 0.3rem 0.5rem;
  border-radius: 3px;
  display: inline-block;
}

.action-buttons a {
  margin-right: 0.5rem;
}

  </style>
</head>
<body>
  

  <div class="container">
    <header class="animated">
      <h1>Edit Movie</h1>
      <p>Update the details of your movie</p>
    </header>

    <div class="form-container animated delay-1">
      <h2 style="margin-bottom: 1.5rem; color: var(--lighter);">Edit Movie Details</h2>
      <form action="/updateMovie" method="post" enctype="multipart/form-data">
        <input type="hidden" name="id" value="<%= movieData._id %>" />
        <div class="form-grid">
          <div class="form-group">
            <label for="title">Movie Title</label>
            <input
              type="text"
              id="title"
              name="title"
              placeholder="Enter movie title (e.g., Chhaava)"
              value="<%= movieData.title %>"
              required
            />
          </div>

          <div class="form-group">
            <label for="director">Director</label>
            <input
              type="text"
              id="director"
              name="director"
              placeholder="Enter director's name (e.g., Laxman Utekar)"
              value="<%= movieData.director %>"
              required
            />
          </div>

          <div class="form-group">
            <label for="releaseYear">Release Year</label>
            <input
              type="number"
              id="releaseYear"
              name="releaseYear"
              placeholder="Enter release year (e.g., 2025)"
              value="<%= movieData.releaseYear %>"
              min="1900"
              max="2030"
              required
            />
          </div>

          <div class="form-group">
            <label for="rating">Rating (0-10)</label>
            <input
              type="number"
              id="rating"
              name="rating"
              placeholder="Enter rating (e.g., 9.5)"
              value="<%= movieData.rating %>"
              step="0.1"
              min="0"
              max="10"
              required
            />
          </div>

          <div class="form-group">
            <label for="duration">Duration</label>
            <input
              type="text"
              id="duration"
              name="duration"
              placeholder="Enter duration (e.g., 2h 41m)"
              value="<%= movieData.duration %>"
              required
            />
          </div>

          <div class="form-group">
            <label for="image">Poster Image</label>
            <input type="file" id="image" name="image" accept="image/*" />
            <img src="<%= movieData.image %>" class="movie-poster" alt="<%= movieData.title %>" />
          </div>

          <div class="form-group full-width">
            <label for="description">Description</label>
            <textarea
              id="description"
              name="description"
              placeholder="Enter movie plot description (e.g., historical action film based on the life of Sambhaji Maharaj.....)"
              required
            ><%= movieData.description %></textarea>
          </div>
        </div>

        <div style="text-align: right; margin-top: 1rem;">
          <button type="submit" class="btn btn-primary">
            <i class="fas fa-save"></i> Update Movie
          </button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>